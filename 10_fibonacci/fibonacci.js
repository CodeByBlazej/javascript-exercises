const fibonacci = function(x) {
  let fib = [];
for (let i = 0; i < x; i++){
  x = [i - 1] + [i - 2]
  
  // fib.push(x)
  // num = (num - 1) + (num - 2)
}

return fib;
};

// Do not edit below this line
module.exports = fibonacci;
